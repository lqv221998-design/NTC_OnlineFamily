<Window x:Class="NTC.Revit.Views.UploadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Batch Family Upload" Height="600" Width="800"
        WindowStartupLocation="CenterScreen"

        Background="{DynamicResource BackgroundBrush}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/NTC.Revit;component/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Drop Zone -->
            <RowDefinition Height="*"/>    <!-- Data List -->
            <RowDefinition Height="Auto"/> <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- 1. HEADER -->
        <Border Grid.Row="0" Background="White" Padding="20" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <DockPanel>
                <materialDesign:PackIcon Kind="CloudUpload" Width="30" Height="30" Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center" Margin="0,0,15,0"/>
                <StackPanel>
                    <TextBlock Text="Batch Family Upload" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextDarkBrush}"/>
                    <TextBlock Text="Upload multiple .rfa files to NTC Cloud" FontSize="12" Foreground="{StaticResource TextLightBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- 2. DROP ZONE -->
        <Border Grid.Row="1" Margin="20" Height="100" 
                CornerRadius="8" Background="White"
                AllowDrop="True">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="BorderBrush" Value="#BBBBBB"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <!-- Dashed Border Effect -->
                    <Setter Property="Border.Effect">
                         <Setter.Value>
                             <DropShadowEffect ShadowDepth="0" Color="#DDDDDD" BlurRadius="10"/>
                         </Setter.Value>
                    </Setter>
                </Style>
            </Border.Style>
            
            <!-- Dashed visual created by StrokeDashArray on a Rectangle inside if needed, or simple Border style -->
            <!-- Using simple content for clean look -->
            <Button Command="{Binding SelectFileCommand}" Style="{StaticResource MaterialDesignFlatButton}" Height="Auto" Background="Transparent" BorderThickness="0">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="FileUploadOutline" Width="40" Height="40" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Drag &amp; Drop .rfa files here" FontSize="16" Foreground="{StaticResource TextLightBrush}" Margin="0,10,0,5"/>
                    <TextBlock Text="(Or click to browse)" FontSize="12" Foreground="#AAAAAA" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Border>

        <!-- 3. DATA LIST (Files) -->
        <Grid Grid.Row="2" Margin="20,0,20,10">
            <DataGrid IsReadOnly="True"
                      ItemsSource="{Binding Files}"> <!-- Placeholder binding for Batch collection -->
                <DataGrid.Columns>
                    <DataGridTextColumn Header="File Name" Width="*" Binding="{Binding FileName}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                    <DataGridTextColumn Header="Category" Width="150" Binding="{Binding Category}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                    <DataGridTextColumn Header="Version" Width="100" Binding="{Binding RevitVersion}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                    <DataGridTemplateColumn Header="Status" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <materialDesign:Chip Content="{Binding Status}" FontSize="11" Height="24"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Action" Width="80">
                         <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource MaterialDesignIconForegroundButton}" ToolTip="Remove">
                                    <materialDesign:PackIcon Kind="Close" Width="16" Height="16"/>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
                
                <!-- Zero State / Empty State -->
                <DataGrid.Style>
                    <Style TargetType="DataGrid" BasedOn="{StaticResource AlphaDataGridStyle}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Files.Count}" Value="0">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Style>
            </DataGrid>
        </Grid>

        <!-- 4. FOOTER -->
        <Border Grid.Row="3" Background="White" Padding="20" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <ProgressBar Width="200" Height="6" IsIndeterminate="True" 
                                 Foreground="{StaticResource PrimaryBrush}"
                                 Visibility="{Binding IsUploading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    <TextBlock Text="{Binding StatusMessage}" Margin="15,0,0,0" VerticalAlignment="Center" FontStyle="Italic" Foreground="Gray"/>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,10,0" Height="40" BorderBrush="#CCCCCC" Foreground="Gray"/>
                    <Button Command="{Binding SubmitCommand}" Content="Upload All" Style="{StaticResource AlphaButtonStyle}" Width="150" Height="40"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
