<Window x:Class="NTC.Revit.Views.UploadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:NTC.Revit.ViewModels"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Smart Upload - NTC Online Family" Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        d:DataContext="{d:DesignInstance vm:UploadViewModel, IsDesignTimeCreatable=False}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <TextBlock Grid.Row="0" Text="Upload Family" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,20"/>

        <!-- FILE SELECTION -->
        <materialDesign:Card Grid.Row="1" Padding="16" UniformCornerRadius="6">
            <StackPanel>
                <Button Command="{Binding SelectFileCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Content="Select .rfa File"/>
                <TextBlock Text="{Binding FilePath}" TextTrimming="CharacterEllipsis" Margin="0,10,0,0" FontStyle="Italic" Foreground="Gray"/>
                
                <!-- AUTO DETECTED BADGES -->
                <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                    <materialDesign:Chip Content="{Binding DetectedVersion, StringFormat='Ver: {0}'}" 
                                         Icon="GetApp" Background="{DynamicResource PrimaryHueLightBrush}"
                                         Visibility="{Binding DetectedVersion, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                </StackPanel>

                <!-- ERROR MESSAGE -->
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,10,0,0"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- FORM DATA -->
        <StackPanel Grid.Row="2" Margin="0,20,0,0" IsEnabled="{Binding IsUploading, Converter={StaticResource InvertBooleanConverter}}">
            <TextBox Text="{Binding FileName}" materialDesign:HintAssist.Hint="Family Name" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,10"/>
            <TextBox Text="{Binding Category}" materialDesign:HintAssist.Hint="Category (e.g., Doors)" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,10"/>
        </StackPanel>

        <!-- UPLOAD BUTTON -->
        <StackPanel Grid.Row="4">
            <ProgressBar IsIndeterminate="True" Height="4" Margin="0,10" 
                         Visibility="{Binding IsUploading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            
            <TextBlock Text="{Binding StatusMessage}" HorizontalAlignment="Center" Margin="0,5"/>

            <Button Command="{Binding SubmitCommand}" Content="UPLOAD TO CLOUD" 
                    Style="{StaticResource MaterialDesignRaisedDarkButton}" Height="40"/>
        </StackPanel>
    </Grid>
</Window>
