<UserControl x:Class="NTC.Revit.Views.FamilyBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:NTC.Revit.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="400"
             d:DataContext="{d:DesignInstance Type=vm:FamilyBrowserViewModel, IsDesignTimeCreatable=False}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    
    <Grid Background="{DynamicResource MaterialDesignPaper}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER / SEARCH -->
        <Border Grid.Row="0" Padding="10" Background="{DynamicResource MaterialDesignPrimaryMidBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox Grid.Column="0" 
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Search Families..."
                         Foreground="White" CaretBrush="White" 
                         VerticalAlignment="Center" Margin="0,0,10,0"/>

                <Button Grid.Column="1" Command="{Binding LoadDataCommand}"
                        Style="{StaticResource MaterialDesignFloatingActionMiniSecondaryButton}"
                        ToolTip="Refresh Data">
                    <materialDesign:PackIcon Kind="Refresh" />
                </Button>
            </Grid>
        </Border>

        <!-- PROGRESS BAR -->
        <ProgressBar Grid.Row="1" IsIndeterminate="True" Height="4"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <!-- CONTENT -->
        <ListView Grid.Row="2" ItemsSource="{Binding Families}" 
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <materialDesign:Card Width="160" Margin="8" Padding="0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="120"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Thumbnail (Placeholder if null) -->
                            <Image Grid.Row="0" Source="{Binding ThumbnailUrl, TargetNullValue={x:Null}}" 
                                   Stretch="UniformToFill"/>
                            
                            <!-- Info -->
                            <StackPanel Grid.Row="1" Margin="8">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding Category}" FontSize="11" Foreground="Gray"/>
                            </StackPanel>

                            <!-- Actions -->
                            <Button Grid.Row="2" Margin="4"
                                    Command="{Binding DataContext.DownloadFamilyCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Height="30" Content="Download"/>
                        </Grid>
                    </materialDesign:Card>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- STATUS BAR -->
        <StatusBar Grid.Row="3" Background="#EEE">
            <TextBlock Text="{Binding StatusMessage}" FontSize="12"/>
        </StatusBar>
    </Grid>
</UserControl>
