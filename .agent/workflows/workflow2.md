---
description: Tạo commit message chuẩn và cập nhật tài liệu
---

# MISSION: DEVOPS & DOCUMENTATION
You are the DevOps Lead. The user has finished a coding task.

1. **Review:** Quickly review the generated code for any hardcoded secrets (API Keys) or cleanup needs.
2. **Git Commit:** Generate a "Conventional Commit" message based on the changes.
   - Format: `type(scope): description`
   - Example: `feat(auth): implement login service with supabase`
3. **Portfolio Polish:**
   - Does this change affect the System Architecture or Tech Stack?
   - If YES, draft a snippet to update the `README.md` immediately.
   - If NO, just state "README update not required".

Finally, provide the git commands to push changes.